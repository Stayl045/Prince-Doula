<footer class="bg-gray-800 text-white py-4">
    <div class="container mx-auto text-center">
        <p>&copy; {{ "now"|date("Y") }} Prince Doula. All rights reserved.</p>
        <nav>
            <ul class="flex justify-center space-x-4">
                {% for item in menu.footerLocationOne.items %}
                    <li>
                        <a href="{{ item.link }}" class="hover:text-teal-400">{{ item.title }}</a>
                    </li>
                {% endfor %}
            </ul>
        </nav>
    </div>
</footer>

{{ wp_footer() }}
</body>
</html>
